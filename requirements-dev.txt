# Dependencias SOLO para herramientas de research locales (no runtime del bot).
# No se instalan en Railway. Usadas por scripts/scoring_backtest.py,
# scripts/scoring_optimizer.py y scripts/ml_diagnostic.py.
#
#   pip install -r requirements-dev.txt
#
# (psycopg2-binary y cryptography ya vienen en requirements.txt)
optuna
pandas
sqlalchemy
numpy==2.4.6
# scikit-learn DEBE coincidir EXACTO con requirements.txt (prod): el .joblib
# que genera ml_train.py se carga en Railway con esa misma versión o joblib.load
# falla. Usado por ml_diagnostic.py / ml_stability.py / ml_train.py.
scikit-learn==1.9.0
joblib==1.5.3
